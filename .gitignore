# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Secrets: set these in Railway's Variables tab instead
.env
.env.*
!.env.example

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs / temp
*.log
*.tmp
.cache/
node_modules/

# Local build artifacts
dist/
build/

# Stale concept folder (sandbox couldn't unlink — safe to delete locally)
v2/

# Build artifacts left over from sandbox renders
og-template.html
.gbp-report-build.html
.payment-action-build.html

# GBP client deliverables — for handoff, not for the website repo
GBP/
GBP.zip
NWD-GBP-Report.pdf
NWD-Payment-Action-Sheet.pdf

# Internal previews and renders — never tracked
Claude outputs/

# Internal deliverables, audits, and build files.
# Anything tracked inside public/ is served live at nwtees.com; keep these out
# of public/ entirely. Root-level files are no longer served, but these are
# still client deliverables that belong in Cowork/iCloud, not the repo.
deliverables/
handoff/
NWD-*.pdf
AGENT-READINESS.md
CODE-REVIEW.md
MOBILE-READINESS.md
.og-build.html
.wordmark-test.html
.payments-rationale-build.html
