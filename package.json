{
  "name": "nwd-redesign",
  "version": "1.0.0",
  "private": true,
  "description": "North West Design — custom apparel shop website (Clarkston, WA).",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "scripts": {
    "start": "serve public --config ../serve.json -l tcp://0.0.0.0:${PORT:-8080} --no-clipboard"
  }
}
